# web-ifc 0.0.50 source recovery

The current user remediation prompt authorizes retaining IFC and recovering exact
source evidence. PR #65 accepted the consolidation checkpoint; it did not authorize
distribution, accept this variant, or approve every alternative.

F06-FE-WEBIFC's narrow source-evidence closing condition is demonstrated for review.
The proposed variant retains all 13 package files unchanged, including browser,
Node and multithreaded assets. Classification may move from selection blocker to
an outstanding distribution obligation for this variant. The original candidate
finding remains historically accurate. No general IFC product, browser 3D,
multithreaded runtime, legal-clearance or independent compiler-rebuild claim follows.

## Retained source and concrete availability stage

[The exact lock](../../build-support/frontend/webifc-sources.json) identifies the
main source at npm gitHead b55d8bde10067415d4536b23c27edcc13acf217c and all eight
active FetchContent revisions. Every one of the main archive's 132 file blobs
matches the historical publisher tree bdda258d5fbfe4abebdbc057d58e17436dc9c585.
The original npm package, source package.json and README agree byte-for-byte.
Acquisition uses exact historical codeload URLs and retained GitHub commit/tree
responses, with hashes and retrieval metadata. Live web-cache lookups failed;
direct authoritative publisher retrieval succeeded and remains retained.

The private local source/notice stage is:
`build-worktrees/frontend-qgis-remediation/webifc/source-stage-03`.
It contains the actual nine source archives, original npm package, publisher
records, source-member manifest, 17 unchanged notice files and a source-availability
statement. `success.json` binds its checked full stage manifest and source index.
The stage is an actual local copy, not a promise of future source acquisition.
[Compact evidence](../verification/frontend-qgis-remediation/webifc-evidence.json)
and [source index](../verification/frontend-qgis-remediation/webifc-source-index.json)
bind the retained paths and all package assets.

Historical notices include MPL-2.0 for web-ifc/CDT/fuzzy, fastfloat's
Apache/MIT/Boost alternatives, MIT TinyNURBS/TinyCppTest, GLM's exact alternative
terms, ISC earcut, MIT spdlog and its bundled fmt notice. No license option is
silently selected or applied to newer/older bytes. Nested notices for unused
archive content remain preserved too.

Distribution still requires the applicable notices, actual delivery of covered
sources through a controlled channel, review of archive examples/fixtures before
public source redistribution, and generated-runtime/toolchain obligations.
No binary/source download was published externally. Source custody does not
approve redistribution of every archive member.

## Compile, link and generated binding correspondence

The historical [CMake source](https://github.com/ThatOpen/engine_web-ifc/blob/b55d8bde10067415d4536b23c27edcc13acf217c/src/wasm/CMakeLists.txt)
selects the same 12 C++ translation units for three WASM targets. Its public
includes select the pinned TinyNURBS, fastfloat, CDT, GLM, earcut, fuzzy and spdlog
trees. TinyCppTest is used by the native tests. The actual native compiler's
dependency files identify 241 selected third-party headers, including three
bundled fmt headers. Selected includes use the pinned external GLM; unrelated
TinyNURBS/CDT test gitlinks and fuzzy's bundled GLM copy are not compiled.
Original source archives and their notices remain intact.

| Target | Recorded link contract | Selected output mapping |
|---|---|---|
| web-ifc | --bind, -flto, REAL_T_IS_DOUBLE, growing memory/max 4GB, stack 5MB, modular WebIFCWasm, ENVIRONMENT=web | web-ifc.wasm; browser API bundles generated glue |
| web-ifc-node | Same common flags; no ENVIRONMENT restriction | web-ifc-node.wasm and web-ifc-api-node.js |
| web-ifc-mt | Common flags plus compile/link -pthread, PTHREAD_POOL_SIZE=navigator.hardwareConcurrency, ENVIRONMENT=web,worker | web-ifc-mt.wasm, worker JS; browser API selects MT under its historical environment conditions |

Browser and Node WASM have identical SHA-256
`94e1927131654a4288f8b868f33766c33979a8290d019ad3b9def7f40a0afbd6`.
That equality does not establish which named target originally generated either
copy. MT WASM is
`797f6e0be82d95c22292894e8c9c8e8e70eb46d20b519b3f081a8df1e91208ca`.
All 35 C++ embind function names occur in each selected WASM; all 35 are available
in the executed Node module. This is corroborating mapping evidence, not a proof
that an arbitrary binary containing those strings was generated by that source.

The retained TypeScript API, schema generator, IFC2X3/IFC4/IFC4X3 EXPRESS inputs,
generated schema TS/C++ and package scripts document the binding chain. All 1,137
numeric IFC exports match between source TS and shipped declaration names/values.
Historical package scripts use tsc for declarations and esbuild for browser/Node
bundles, with __WASM_PATH__ selecting generated glue. The exact package-lock is
retained. Historical Dockerfile/publish/test workflows specify Emscripten 3.1.44;
they are evidence, and were not installed or executed. Publisher npm/git/tree
identity, exact source and dependency custody, declarations/bindings and usable
native/retained-WASM probes jointly support the narrow corresponding-source
finding. An independent byte-identical WASM rebuild remains undemonstrated.

## Executed bounded probes and failures

`native-03` is the final frozen recipe run. It extracts the locked sources into
fresh directories and runs the unmodified historical CMake source with local
FETCHCONTENT_SOURCE_DIR overrides and FETCHCONTENT_FULLY_DISCONNECTED. CMake's
inherited fuzzy src-to-fuzzy rename occurs only inside the fresh working copy.
The recipe enables the host CMake minimum-policy compatibility setting; it does
not patch source or remove diagnostics. Exact compiler/tool identities,
compile_commands.json, link commands, source/header maps and warning logs are
retained. Frozen tooling and all 3,190 extracted source/dependency file bytes are
verified again before success, permitting only the historical fuzzy directory
rename. Native parser/geometry compilation and all six upstream encoding tests
pass. The Emscripten-only web-ifc-api.cpp is not compiled by the native target.

The same run executes a first-party synthetic IFC2X3 rectangular extrusion using
the unchanged shipped Node API/WASM: one 2×3×4 metre box, one mesh, 12 triangles,
finite coordinate bounds, model close and save/reopen all pass. The fixture has
no external model inputs. This probes actual loading/geometry/API behavior; it is
not browser integration or MT acceptance. The frontend agent independently
verifies unchanged IFC bytes in the new replay bundle and performs that bundle's
required viewer smoke.

Each native configure/build/test and WASM command runs under the existing
Internet-socket-denying runner, with successful command and kernel proof records.
No daemon, listener, credential or security-setting change is introduced.
There are 13 source-stage positive/negative guards, including exact dependency
revision, package-byte and binding mismatch rejection. Failed/reused stage paths
cannot overwrite prior evidence; an index is not success without its receipt.

Earlier evidence remains retained: wasm-01 failed on an incorrect Node path.
wasm-02 exited zero but emitted IFC errors because the synthetic rectangle
omitted its explicit 2D placement; it is a failed probe, not acceptance.
The corrected fixture and diagnostic guard pass wasm-03 and final native-03.
Native-01 and native-02 also compiled and passed six tests; native-03 supersedes
them with a frozen replayable harness and complete post-build source integrity. source-stage-01/02 are retained development stages,
not the final proposed availability stage. Warnings are kept verbatim.

## Replay

From the platform repository, choose fresh output names:

```sh
python3 build-support/frontend/webifc_sources.py \
  --retained /home/revelberry/Projects/AmbisGIS/build-worktrees/frontend-qgis-remediation/webifc \
  --stage /home/revelberry/Projects/AmbisGIS/build-worktrees/frontend-qgis-remediation/webifc/source-stage-NEW \
  --frontend-package /absolute/verified/frontend/dist/js/web-ifc

python3 build-support/frontend/webifc_native_probe.py \
  --source-stage /absolute/source-stage-NEW \
  --output /absolute/new-native-probe \
  --frontend-package /absolute/verified/frontend/dist/js/web-ifc \
  --node /absolute/retained/node

python3 -m unittest discover -s build-support/frontend -p test_webifc_sources.py -v
```

Final owner adoption and distribution review remain separate. FND-08 covers the
full retained toolchain/generated-runtime build and independent repair, without
reopening this row into a demand for a byte-identical compiler bootstrap.
