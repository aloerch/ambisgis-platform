# F02-06 QGIS optional-resource variant

The current owner remediation prompt authorizes this **new private candidate
variant**. PR #65 accepted the consolidation checkpoint, not these new artifacts,
owner adoption or distribution permission. The original owned archive,
`build-06/prefix`, its manifest and historical receipts remain unchanged.

The [evidence index](../verification/frontend-qgis-remediation/qgis-evidence.json)
binds every new receipt, executed tool, full inventory, failure and preview.
The selected stage is
`build-worktrees/frontend-qgis-remediation/qgis/selection-03/prefix` beneath
`/home/revelberry/Projects/AmbisGIS`. Its full manifest SHA-256 is
`4e65c76d2af1bc811dca913d0450d4ab614f5cf14c3c9022d544796e5130a8a3`.
The original manifest remains
`931e5f0523e1d9bdba6b2fbd27e6c208297456db696265beb961a57e77ed82c9`.
The source is unchanged owned QGIS `1a4cda5f2620e7374e5926fc955a7d2d06493e15`.

The deterministic [selection recipe](../../build-support/qgis/resource_selection.py)
derives exact SVG rows from the hash-bound original manifest and the recorded
notice scopes, verifies each excluded SVG against the retained owned tar, and
fails any count/notice/overlap mismatch. It removes exactly **1,129 named files**:
256 under the 19 SRC-01 scopes, 138 under SRC-02's two scopes, 690 under SRC-03,
and 45 under SRC-04. All 265 ColorBrewer palettes remain byte-identical.
No replacement collection or source/binary rebuild was needed.

The new tree has 8,110 entries versus 9,235 originally. All 8,003 unaffected
entries retain their exact bytes/symlink targets and modes. Eighty-four collection
metadata/notice files are relocated unchanged outside the active palette archive;
19 catalogues remove only references to omitted collections. The exact original
icon/font terms and a separate acknowledgement/readme are added. Existing source
archives remain custody records, not approved public distribution bundles.
ColorBrewer's exact acknowledgement/naming terms and all other untargeted
obligations remain open. This product includes color specifications and designs
developed by Cynthia Brewer (http://colorbrewer.org/).

All source Qt resource definitions contain no cpt-city embedding. A complete
8,103-regular-file magic scan finds no packaged containers; 244 active XML
files and four SQLite/GeoPackage databases have no omitted scheme references.
Runtime additionally enumerates Qt resources and verifies the actual new
`prefixPath`, `pkgDataPath`, palette archive, native libraries and providers.
The prior stage cannot supply missing palettes. Two independent resource stages
have identical selected file inventories; this is packaging replay evidence,
not a byte-identical QGIS rebuild.

**SRC-02 remains blocked.** The retained `gmt/GMT_dem1.svg` is byte-identical to
omitted `td/DEM_print.svg`, SHA-256
`0ba1cad3e42202036ab6a86663a09d377eb84eb22584df0e4dbc9fdee54cf602`.
The GMT notice records different authors and GPLv2/distribute=yes; the td notice
records freeuse/distribute=no. Byte identity alone settles neither provenance nor
which notice applies. The recipe explicitly records this exact alias and rejects
any other unexpected copy. It does not delete a 1,130th palette or claim all
excluded bytes vanished. The smallest remaining remedy is establish the exact
GMT source/permission chain or obtain an explicit decision on that additional
asset. SRC-01/03/04 have variant-selection closure evidence; original findings
remain applicable to the untouched original stage.

`runtime-07` passes in **21.176 seconds**. Native tests establish 1,129 failed file
opens and author-model lookups, all 265 retained palettes parsed with 530 endpoint
comparisons against source SVG, and actual chooser interaction through Authors →
cb/seq → Blues → 09. Generic gradients, vector/raster styles, native rendered
colors and save/reopen pass. A project carrying an omitted `td/DEM_print` reference
preserves both serialized symbols and identical rendered colors. Its source ramp
file is absent. The inherited `loadFile()` returns true even on a missing file;
that misleading boolean is recorded, never used as availability evidence.
Reclassification from omitted named ramps is unavailable and full saved-project
compatibility is not claimed.

The real staged desktop performs eight completed canvas renders, zoom, native
Save/reopen and File Exit with process exit zero. Native server performs six real
HTTP requests with GetMap layer/raster assertions and one restart; both processes
exit zero. Before/after stage and dependency integrity, 79 parent plus 79 child
loopback probes, invalidated fixture credentials, scrubbed files and shutdown all
pass. The existing PostgreSQL-only supervision exception remains explicit.
Offscreen captures were viewed; no physical-display, broad accessibility,
full renderer/provider/OGC, publishing or release acceptance is inferred.

Fresh checks pass **71 QGIS harness tests**, including 14 new selection/path
regressions, and **15 shared supervision regressions**. The historical 66 C++ and
16 Python native cases remain unchanged evidence, not new runs. Root integration
runs the plan/candidate checks independently.

All failed attempts remain: `selection-01` discovered the GMT alias;
`runtime-01` caught the invalid loadFile assumption; `runtime-02/04` exposed the
inherited SIP owning-wrapper conversion for borrowed root-item vectors (the
witness now uses the same QModelIndex API as the native browser);
`runtime-03` caught SVG's implicit black endpoint; `runtime-05/06` caught a chooser
fixture that did not make the real UI selection. A controller startup typo and
one bounded probe wrapper-type failure are also indexed. `resource-probe-02`
then passed before the full final run. No failed receipt was rewritten as success.

The frozen selection tooling precedes execution; its command/start/finish receipt
and before/after hashes bind `selection-03`. Runtime copies and checks every
executed helper; final reviewed files equal that snapshot. Independent review
also verified the successful receipts and retained source hashes.

Replay into fresh directories using the exact baseline/profile (the commands'
absolute inputs are retained in `selection-03-execution.json` and
`runtime-07-execution.json`):

```sh
python3 build-support/qgis/resource_selection.py --help
python3 build-support/qgis/runtime.py --help
python3 -m unittest discover -s build-support/qgis -p 'test_*.py' -v
```

A later nonidentical QGIS build requires a reviewed update of the baseline
inventory pin while retaining exact source/notice/selection guards. This recipe
is a versioned packaging step, not a one-off deletion of an installed candidate.
The next batch remains the coordinated Java work and the finite GMT alias remedy;
FND-02 and final adoption/distribution gates remain open.
