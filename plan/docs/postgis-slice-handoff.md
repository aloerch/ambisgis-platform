# FND-02 PostgreSQL/PostGIS build slice — live resumption record

Work continues on platform `fnd-02/source-evidence` for open PR #51; do not merge without owner approval. Integration base PR #50 was verified merged at `3e7581a06b000856ea9b27468cc415612c28b4b0`; starting feature head was `b68efb58b9ddb139f98c3f460e65f06c2573a668`. Live FND-02/07/08/GOV-02 issue discussion and PR reviews were read; no reviews existed. FND-02 remains In progress; FND-07/08 dependencies remain unsatisfied. GOV-02 remains Blocked on the existing saved-view UI steps.

Actual platform worktree: `/home/revelberry/Projects/AmbisGIS/ambisgis-platform-source`. The initial plan workspace is a separate checkout with no remote. Preserve both and existing source no-checkout repositories/worktrees.

Retained inputs: `/home/revelberry/Projects/AmbisGIS/source-archives/postgis-slice`. Build run: `/home/revelberry/Projects/AmbisGIS/build-worktrees/postgis-slice/run-001`, with isolated `prefix`, `sources`, `build`, `logs/commands.jsonl`. No global installations. Large archives, generated sources/binaries and full logs remain outside Git. Dependency manifest/acquisition code is integrated at `build-support/postgis/inputs.json`; 19 inputs and 92 notice records passed independent verification.

Exact owned sources: PostgreSQL15.19 `2ff1375b5dd8bf09d8cb0e795974528180fd75ca`; PostGIS3.5.7 `9816f82458db774e62906cfb2c4f01f8b262c862`; real earlier upgrade fixture PostGIS3.5.6 `9aa71a8e5059959929825926db5ebefafde471db`. Candidate identity is not production approval. No changes to owned source code or defaults are planned by this build slice.

Profile: PostgreSQL with retained zlib/libxml2, no readline, SSL/ICU/LLVM/TAP disabled for this isolated UNIX-socket experiment. TLS/security/deployment profile remains a separate gate. PostGIS geometry/geography, raster, topology, MVT/Geobuf/Wagyu and GeoJSON enabled; GUI, SFCGAL and address standardizer intentionally excluded. GDAL local raster drivers include GTiff/PNG/JPEG/MEM/VRT/EHDR and small regression fixture drivers. PROJ network is disabled; retained source SQL generates proj.db, with source-contained test grids; global PROJ-data coverage remains incomplete.

Recipes: `build-support/postgis/build.py`, `validate_database.py`, `offline_exec.py`. Build driver verifies archive hashes, preserves per-command working directory/environment/exit/log hashes and refuses silent required-feature/CUnit omission. CUnit's self-test always returns zero; the driver additionally checks positive assertion count and zero failures. Its distributed config.status rejects out-of-tree configure, so the recipe reconfigures the isolated extracted copy in place. First failure and subsequent result remain logged.

Meaningful checkpoint: PostgreSQL15.19 with retained XML/zlib passed217 core tests; zlib, libxml2, CUnit3915 assertions, GoogleTest63, json-c25, JPEG, PNG37, TIFF154, GEOS493, PROJ55, protobuf and protobuf-c tests passed. GDAL and both PostGIS versions remain in progress; no SQL or earlier-version upgrade result is claimed yet. The initial PG regression socket path exceeded107 bytes; the recipe now supplies a unique short PG_REGRESS_SOCK_DIR and preserves failed-start logs. Protobuf21.12 had missing generated-header build dependencies; an explicit retained-protoc generation step precedes parallel compilation without changing tests. Do not infer later results from this record. Inspect `logs/commands.jsonl` and running process argv before resuming. Do not launch overlapping builds against one prefix.

Next sequence: finish dependencies and PG15.19; build/install PostGIS3.5.6; run `validate_database.py prepare-upgrade`; install3.5.7; run `finish-upgrade --upgrade-state <observed path>`; actual source and extension regression suites; then clean retained-input rebuild in a new run directory using offline_exec. The offline wrapper applies inherited Linux seccomp denial of non-UNIX sockets, preserving local PostgreSQL; host tools/libraries remain unretained and this is not whole-product disconnected repair acceptance.

Package checks must run from `plan/` with `PATH=/tmp/ambisgis-validation-venv/bin:$PATH` (existing validation venv includes jsonschema). System python lacks jsonschema; schema skips/failure using that environment are not a passing strict validation result.
