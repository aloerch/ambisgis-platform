# F02-06 one-file QGIS GMT successor

The owner Java/GMT continuation prompt, sections 2.A and 5, authorizes this
implementation. The #66 merge alone does not. **F06-QGIS-SRC-02 has bounded
selection closure for this new proposed resource variant.** Original provenance
uncertainty, ColorBrewer obligations, adoption and distribution gates remain.
FND-02 remains In progress; all four criteria and eight pass conditions are unchanged.

The [evidence index](../verification/java-gmt-remediation/qgis-evidence.json)
binds every new command, receipt, executed helper, full inventory and capture.
Repository `aloerch/ambisgis-platform`, branch
`fnd-02/java-gmt-selection-remediation`, integration base
`6b2e2fd7edc91746748d916d34af01f0a681e342`. The root integration handoff binds
final commit/PR; this component made no separate GitHub writes.

## Selection and preserved source

The stage is `/home/revelberry/Projects/AmbisGIS/build-worktrees/java-gmt-remediation/qgis/selection-01/prefix`.
It derives from verified `frontend-qgis-remediation/qgis/selection-03`, through
the existing deterministic recipe's explicit `--parent-selection` mode.
This mode pins the parent manifest and selection receipt; the original recipe
mode remains available. Owned QGIS source stays at
`1a4cda5f2620e7374e5926fc955a7d2d06493e15`.

Exactly `share/qgis/resources/cpt-city-qgis-min/gmt/GMT_dem1.svg` is additionally
removed: 591 bytes, SHA-256
`0ba1cad3e42202036ab6a86663a09d377eb84eb22584df0e4dbc9fdee54cf602`.
Its seven explicit catalogue references are removed; the remaining GMT collection
and original GMT notice remain unchanged. The resource README changes only its
exclusion count. No source recompilation occurs.

The cumulative exclusion is **1,130 named palettes** (256/139/690/45 for
SRC-01/02/03/04). The new inventory has 8,109 entries; 8,101 entries retain exact
parent bytes, symlink targets and modes. All 56 bin/lib/shared-module entries
and all **265 ColorBrewer** palettes are unchanged. The full selected-stage
same-byte scan finds no alternate excluded-palette copies. All 244 active XML
files and four SQLite databases have no omitted references; 8,102 regular files
have no packaged containers. Thirteen source Qt resource definitions and fresh
native Qt resource enumeration expose no embedded cpt-city copy.

Identical bytes do not establish identical licensing. This authorized exclusion
avoids unresolved provenance without declaring the GMT grant invalid. Original
archives, notices, build-06 and selection-03 remain unchanged and retain their
original restrictions. ColorBrewer's exact acknowledgement and naming conditions
remain: This product includes color specifications and designs developed by
Cynthia Brewer (http://colorbrewer.org/).

| Record | SHA-256 |
|---|---|
| Parent selected-stage manifest | `4e65c76d2af1bc811dca913d0450d4ab614f5cf14c3c9022d544796e5130a8a3` |
| New selected-stage manifest | `a2152edc50a7fdb34401e148c1eec238dfdf5dad6600faf73e92a063a4c47baa` |
| New cumulative selection | `a1b259c8e422d52f63aba093d49af915301c6b4b7c11bb134d0a600d31f6e43c` |
| Executed resource selection recipe | `03a0562feeca2201c1882c9f4b45b72885a814c1ff89fb2d6019f21278079954` |
| Fresh runtime-01 result | `2a61bfa0d2ab96851fc2df393589a9b260cda6c915b65f2d5f35a081aecf2e0d` |
| Fresh native resource result | `8b0e8e2536024d95042f41021a54d5a3a6286e5f09173d4ecc799ead6ffe2791` |

## Fresh execution and reused evidence

`runtime-01` passes in 24.102 seconds against that actual new stage. Native QFile
opens and author-model lookup reject all 1,130 omitted palettes. All 265 retained
ColorBrewer palettes parse, with 530 endpoint comparisons against source SVG.
The real chooser selects Authors → cb/seq → Blues → 09. Generic gradients and
vector/raster styling, native rendered colors and save/reopen pass.

Two saved-project witnesses independently carry `td/DEM_print` and
`gmt/GMT_dem1`. Reopening preserves explicit serialized symbols and identical
rendered colors with both source-ramp files absent. Inherited `loadFile()` still
returns true on a missing file; availability assertions use actual QFile failure
and native model absence. Reclassification using the omitted ramps is unavailable;
this is not full saved-project compatibility acceptance.

The staged desktop completes seven canvas-render events, zoom, native Save/reopen
and native File Exit with process exit zero. Native QGIS server performs six real
HTTP requests with known vector/raster/layer assertions and one restart; both
server processes exit zero. Actual prefix/package/archive/library/provider paths
select the new stage and cannot borrow omitted palettes from old stages.

Unchanged loopback controls pass 79 parent plus 79 child probes. The pre-existing
PostgreSQL-only supervision exception remains explicit; no new exception was
introduced. Task processes stopped, fixture credentials were invalidated, private
files scrubbed, and the secret scan passed. Original/parent/new stage inventories,
source archive and executed tooling hashes passed final integrity checks.

Fresh harness checks: **75 QGIS and 15 loopback guards**. The original compiled
binaries and historical **66 C++/16 Python native cases** are reused with integrity
verification; those whole native suites were not rerun. Screenshots were viewed.
No physical-display, broad accessibility, full provider/cartographic/OGC,
multi-platform, publication or release acceptance is implied.

No QGIS implementation/runtime attempt failed in this batch. The IDE image viewer
failed to create its bwrap namespace; the same images were read through approved
file access and viewed without weakening controls. Host `rg` was unavailable for
one read-only process check; a subsequent grep check worked. Prior failed QGIS
attempts remain linked through the unchanged historical evidence index.

## Exact replay

From this checkout, on the verified host, use new output directories:

```sh
/usr/bin/python3.13 build-support/qgis/resource_selection.py \
  --baseline /home/revelberry/Projects/AmbisGIS/build-worktrees/frontend-qgis-remediation/qgis/selection-03/prefix \
  --manifest /home/revelberry/Projects/AmbisGIS/build-worktrees/frontend-qgis-remediation/qgis/selection-03/after-manifest.json \
  --parent-selection /home/revelberry/Projects/AmbisGIS/build-worktrees/frontend-qgis-remediation/qgis/selection-03/selection.json \
  --source-archive /home/revelberry/Projects/AmbisGIS/source-archives/qgis-candidate/owned/qgis-3.44.14.tar \
  --output /home/revelberry/Projects/AmbisGIS/build-worktrees/java-gmt-remediation/qgis/selection-02
/usr/bin/python3.13 build-support/qgis/runtime.py \
  --config /home/revelberry/Projects/AmbisGIS/build-worktrees/java-gmt-remediation/qgis/runtime-config-01.json \
  --output /home/revelberry/Projects/AmbisGIS/build-worktrees/java-gmt-remediation/qgis/runtime-02
python3 -m unittest discover -s build-support/qgis -p 'test_*.py' -v
```

The runtime command deliberately tests the already selected selection-01. To test
another stage, derive a fresh config replacing every stage path and both resource
hashes, preserving all other pinned profile inputs; retain its new invocation.
The IDE invokes host commands through `flatpak-spawn --host`. The root integrator
updates the shared candidate/finding/combination records and package checks.

## Delegated final Java/PostGIS/GWC runtime witness

The integrator separately delegated `build-support/java/remediation_mosaic.py`
and the full exact-WAR GWC runtime to this work lane. The accepted final output is
`/home/revelberry/Projects/AmbisGIS/build-worktrees/java-gmt-remediation/gwc-05`,
using aggregate-02 WAR SHA-256
`e291629c38cab29eed207d1f88b2cbb747c8ca418c4737f10e5324ebc0aae044` and
`runtime-profile-2.json`. Controller and backend both pass. The pinned detailed
record is [qgis-mosaic-final.json](../verification/java-gmt-remediation/qgis-mosaic-final.json).
Controller receipt SHA-256 is
`525e290c9523225b2abab422e51a3d5372b8a440db50e3269fdb1d021d5217a4`;
backend receipt is
`7e05be9ae2cfd7c44c31f765cc9b1ba27cec3c0fdb09adda159d875290e49af8`.

The helper creates two known-color GeoTIFFs covering `[0,0,4,4]` in EPSG:4326,
uploads them through the native ImageMosaic REST ZIP endpoint, and lets the actual
selected GeoTools/PostGIS provider create and populate its spatial index. SQL
checks independently verify exactly two granules, filenames, source hashes,
SRID, polygon geometry, extents, areas and GiST indexes. A separate ordinary
PostGIS vector table is published through the real datastore/feature REST APIs;
WFS returns the two expected labels and Point coordinates. Direct WMS returns
both known raster colors at their expected positions with transparent pixels
outside the coverage. These checks pass before and after both service restarts.
The WMS PNG is byte-identical across restart, SHA-256
`5da7f751ce2bb2d3b2c07adb335a65815bc6e5ee373eeb7fae9339f2cbbec431`.
The [actual restarted image](../verification/java-gmt-remediation/qgis-mosaic-restarted.png)
was visually inspected.

Both exact deployed WARs report the selected source-built Marlin renderer hash
`14633f73fed7e30553927201309bcaf407a9f5462fce87692f196859f33d074a`,
245 fractional-alpha probe pixels, absent Oracle classes/providers and available
PostGIS providers. Existing GWC assertions pass public/private cold MISS, repeat
HIT and restart HIT, eight anonymous/outsider private-layer denials, invalid-layer
responses and two unchanged cache entries. Security bytes, stateless bearer
configuration, strict roles, deployed WAR, container/launcher, executed tooling
and installed owned backend integrity checks pass. Fresh GWC guard suite: **59
passed**. The earlier IDE invocation without Pillow is retained as an environment
failure; the accepted suite uses the established retained runtime venv.

The same 79 parent plus 79 child loopback probes pass, with the unchanged
PostgreSQL-only exception. Diagnostics report zero secret/security failures.
All four service phases stopped; fixture credentials were invalidated; stored
mosaic and vector connection credentials were scrubbed; only the marker-owned
synthetic schema was dropped; PostgreSQL and the supervisor exited zero.

Four aggregate-01 convergence failures remain hash-linked in
[qgis-mosaic-convergence.json](../verification/java-gmt-remediation/qgis-mosaic-convergence.json).
The original external-file request was rejected by the existing URL policy, so
the helper uses the supported inline ZIP API. Native upload validation opens a
real mosaic in a temporary `CoverageStoreFileValidator` directory and leaves its
PostGIS index table after removing those temporary files. Trying a common Name
made the subsequent real index collide with that table. The accepted witness
requires exactly the published index plus one narrowly named validator index,
checks both independently against the two known granules, and removes both in
final schema cleanup. It does not suppress arbitrary extra tables or alter any
server/security control. Those four failed runs all completed credential/schema
and service cleanup; none is final acceptance evidence.

This is bounded synthetic raster/vector/GWC compatibility evidence on the final
candidate. It does not imply production deployment, full format/provider coverage,
release adoption or legal approval. The root integrator owns independent review,
shared candidate assembly and repository/package checks.
