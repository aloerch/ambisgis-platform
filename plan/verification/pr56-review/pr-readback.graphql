query { repository(owner:"aloerch",name:"ambisgis-platform") { p54: pullRequest(number:54) { number state headRefOid baseRefName baseRefOid mergeStateStatus mergeable reviewDecision autoMergeRequest { enabledAt } mergedAt mergedBy { login } mergeCommit { oid } reviewRequests(first:100) { nodes { requestedReviewer { __typename } } pageInfo { hasNextPage endCursor } } reviewThreads(first:100) { nodes { id isResolved } pageInfo { hasNextPage endCursor } } comments(first:100) { nodes { id author { login } body url } pageInfo { hasNextPage endCursor } } reviews(first:100) { nodes { id author { login } state body url } pageInfo { hasNextPage endCursor } } statusCheckRollup { state } } p55: pullRequest(number:55) { number state headRefOid baseRefName baseRefOid mergeStateStatus mergeable reviewDecision autoMergeRequest { enabledAt } mergedAt mergedBy { login } mergeCommit { oid } reviewRequests(first:100) { nodes { requestedReviewer { __typename } } pageInfo { hasNextPage endCursor } } reviewThreads(first:100) { nodes { id isResolved } pageInfo { hasNextPage endCursor } } comments(first:100) { nodes { id author { login } body url } pageInfo { hasNextPage endCursor } } reviews(first:100) { nodes { id author { login } state body url } pageInfo { hasNextPage endCursor } } statusCheckRollup { state } } p57: pullRequest(number:57) { number state headRefOid baseRefName baseRefOid mergeStateStatus mergeable reviewDecision autoMergeRequest { enabledAt } mergedAt mergedBy { login } mergeCommit { oid } reviewRequests(first:100) { nodes { requestedReviewer { __typename } } pageInfo { hasNextPage endCursor } } reviewThreads(first:100) { nodes { id isResolved } pageInfo { hasNextPage endCursor } } comments(first:100) { nodes { id author { login } body url } pageInfo { hasNextPage endCursor } } reviews(first:100) { nodes { id author { login } state body url } pageInfo { hasNextPage endCursor } } statusCheckRollup { state } } p56: pullRequest(number:56) { number state headRefOid baseRefName baseRefOid mergeStateStatus mergeable reviewDecision autoMergeRequest { enabledAt } mergedAt mergedBy { login } mergeCommit { oid } reviewRequests(first:100) { nodes { requestedReviewer { __typename } } pageInfo { hasNextPage endCursor } } reviewThreads(first:100) { nodes { id isResolved } pageInfo { hasNextPage endCursor } } comments(first:100) { nodes { id author { login } body url } pageInfo { hasNextPage endCursor } } reviews(first:100) { nodes { id author { login } state body url } pageInfo { hasNextPage endCursor } } statusCheckRollup { state } } } }
