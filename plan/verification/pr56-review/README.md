# PR #56 preparation receipts

Phase A only, existing gov-02/pr-review-visibility branch. See
[review and handoff](../../docs/pr-56-review.md) for the owner decision and limits.

- `preflight.json`: verified owner/repository/PR identities, actual merges,
  22 worktree heads/statuses and process inspection before integration.
- `merge-output.txt`: original two STATUS conflicts; `merge-verification.json`
  records normal merge parents/tree and deliberate paragraph reconciliation.
- `preservation.json`: Java/database/Jupyter/ADR implementation and inherited
  historical receipts unchanged; governance regression preserved exactly.
- `pr-readback.graphql` / `pr-readback-before.json`: complete discussions,
  reviews, requests and threads; previous #56 CONFLICTING/DIRTY state retained.
- `tooling-tests.*`: current 218-test Java tooling run, exit 0.
- `package-tests.*`: current 176-test plan run, exit 0, including the governance
  preservation regression. No failures/errors/skips in either suite.
- `schemas.*`: strict plan/schema validation output and command receipt.
- `project/`: compact independent read-only audit, raw-file manifest and executed
  script. The old #57 baseline is referenced by immutable commit/hash, not copied.
- `independent-review.txt`: independent scoped engineering/document review;
  this is not owner acceptance.
- `diff-checks.json`: unchanged incoming historical logs cause exit 2 when
  measured against old #56; actual scoped diff against main passes. Raw failing
  output is retained outside Git to avoid duplicating unchanged CRLF evidence.

Fresh raw Project exports, pagination, request bodies, unchanged old-log whitespace
output, and exact-final-head check/publication receipts are retained under
`/home/revelberry/Projects/AmbisGIS/source-archives/pr-review-visibility/preparation-20260920-01/`.
The old `/tmp` audit is not the sole retained copy. Final publication updates only
#56 branch/PR metadata and issue #9 evidence, with no Project mutations or merges.
All native results in earlier reports remain historical; no GIS acceptance is
claimed by package/tooling checks.
