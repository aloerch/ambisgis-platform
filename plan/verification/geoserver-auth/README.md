This directory records the bounded FND-02 configured GeoServer authentication checkpoint. Start with [evidence.json](evidence.json) for exact artifact, receipt, source-manifest and tooling hashes; [the handoff](../../docs/geoserver-configured-auth-handoff.md) explains behavior and remaining acceptance. Raw receipts remain in the retained paths recorded in the index.

The authoritative runs are `aggregate-03`, `aggregate-03-inventory`, `aggregate-03-logging`, `oauth-configured-final-02`, and `configured-final-02`. They bind the same final WAR to complete inventory, logger checks, 708 native cases (707 passed, one inherited skip), and 178 configured/restart assertions covering 177 real HTTP requests. Java tooling passed 366 tests; unchanged plan checks passed 176 tests and four schemas/examples.

The index binds implementation commit `38ef3560a42af230fe75e20cc02578b12d8fcd2f` and Java tree `da58e1ee80e151fe1494acded0b6b3f9a85a32c0`. Packaging, native and logging snapshots contain earlier copies of two HTTP harness files that those runs did not execute; the final executed HTTP snapshot matches the final commit. Scoped reviews retain their own historical identities. The run index preserves earlier failures, including diagnostic leaks, bearer session carryover and the two response-commit native regressions.

Passing component evidence does not close source or product acceptance. The original 48 structurally accounted, 12 unresolved and four partial source dispositions remain; ten servlet runtime artifacts have retained binary/source/POM custody, not independent rebuild or security/license acceptance. Immediate token revocation is not established: the observed native cache permits access until expiry. Human security review and deployment/release gates remain open.
