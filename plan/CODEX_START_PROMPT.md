# Paste into Codex in Visual Studio Code

Implement **AmbisGIS**, the independently maintained GIS product specified in this revision 2 workspace. Read `AGENTS.md`, `README.md`, `REVISION_2_CHANGES.md`, chapters 00/08/09/11/12, `DECISIONS.md`, `repositories.json`, `project.json`, `requirements.json` and `backlog.json`. Read detailed component specifications as tasks require them.

This is **not** an upstream-led integration project. Fork and adopt the selected code, own the product builds and release cadence, and consolidate redundant product models and administration. You may change inherited internals where warranted. Do not enforce thin forks, mandatory upstream merges, plugin-only reliance on externally maintained QGIS/PostGIS, or upstream PR acceptance. Upstream sources/patches are optional inputs after acquisition. Preserve licenses, source provenance, security boundaries and tested storage behavior.

You are authorized to create the **fifteen new public repositories/forks explicitly in repositories.json under my account aloerch**, and the **public user-owned GitHub Project “AmbisGIS — Product Development”** specified in project.json. You may create/update this project's task issues, labels, links and approved workflow metadata, and develop code through feature branches/PRs in those targets. After audit, create `ambisgis/main` from approved source baselines and make it the product default for these newly created forks while preserving donor references.

Do not modify unrelated pre-existing repositories, including osgs-neu or weaveatlas; adopt collisions without verification; delete resources; force-push; publish into upstream namespaces; install secrets or escalate permissions automatically; enable unreviewed workflows; expose private/employer data; contact maintainers; purchase infrastructure; or change production systems. AmbisGIS is a provisional name, not trademark-cleared; binary branding and license decisions require review.

First inspect the environment, GitHub identity and tool permissions. Run package tests and schema/plan validation. Run `python3 tools/bootstrap_repositories.py --owner aloerch` in read-only mode and inspect all fifteen targets. With successful preflight, use `--apply` to create only missing authorized targets and retain the local receipt. On collision/permission/error, stop only that unsafe action and report evidence.

Clone approved targets, verify IDs/remotes, preserve source/license history and seed the plan in ambisgis-platform/plan on a feature branch, with root AGENTS instructions where appropriate. The executable repository bootstrap does not perform seeding, default-branch changes or Project setup.

Implement GOV-01's safe Project importer from chapter 12, using project.json and `python3 tools/export_project_seed.py` as inputs. Inspect current CLI/GraphQL capabilities and permissions; test collision/pagination/retry/progress-preservation behavior before applying. Seed task issues, native dependency relations, fields and views. Store actual remote IDs/URLs, not placeholders. The live Project importer is not already implemented in this package. Request only the owner's local authorization action when truly required, never a token in chat.

Execute P0 evidence tasks, including owned source custody/builds (FND-07/FND-08), identity/authorization, branch correctness, QGIS publishing/rendering and compatibility reuse. Establish source/dependency archives and canonical product branches, not just forks with donor defaults. Run governance tasks alongside unblocked engineering. Then deliver P1's real install/publish/query/share/revoke/restart journey from owned builds. Continue the dependency-linked roadmap in reviewable changes, not one giant scaffold.

Before release, pass canonical-model consolidation, upstream-disconnected rebuild/independent repair, database/REST/UI/notebook/security tests and upgrade/recovery/license gates. Never treat pinning alone as independence or a merged PR as released functionality. End each session with exact code, test and GitHub Project status plus a precise next task.
